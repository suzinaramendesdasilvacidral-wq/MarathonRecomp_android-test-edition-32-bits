<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <!-- OpenGL ES 2.0 -->
    <uses-feature android:glEsVersion="0x00020000" />

    <!-- Touchscreen support -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Game controller support -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <!-- External mouse input events -->
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <!-- Allow access to the vibrator -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- GitHub release checks and hand-off to Android's package installer. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true" >

        <activity
            android:name=".LauncherActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@style/LauncherTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="SDLActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            >
            <!-- The engine always creates a 1280x720 (landscape) window with
                 SDL_WINDOW_RESIZABLE set and no SDL_HINT_ORIENTATIONS hint, which makes
                 SDLActivity.setOrientationBis() fall through to SCREEN_ORIENTATION_FULL_USER
                 (i.e. defer entirely to the phone's own rotation lock/sensor state) instead of
                 forcing landscape - on a phone locked to or currently held in portrait this
                 renders the game sideways. Declaring the orientation here in the manifest
                 takes effect from the very first frame, with no runtime hint/timing race. -->
            <!-- Let Android know that we can handle some USB devices and should receive this event -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
        </activity>

        <!-- Opened from the unified launcher; no separate home-screen icon. -->
        <activity
            android:name=".ModManagerActivity"
            android:label="@string/mod_manager_launcher_name"
            android:exported="false"
            android:theme="@style/ModManagerTheme" />

        <!-- Expose app-owned game files through Android's Storage Access Framework.
             MANAGE_DOCUMENTS is a signature permission: only the system DocumentsUI can
             browse the provider directly, and it grants callers narrow URI permissions. -->
        <provider
            android:name=".MarathonDocumentsProvider"
            android:authorities="${applicationId}.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <!-- Serves only cache/updates/MarathonRecomp_update.apk, read-only, after
             UpdateManager verifies its GitHub digest and signing certificate. -->
        <provider
            android:name=".UpdateFileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>

</manifest>
